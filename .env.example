# Copy to .env (which is gitignored). Both sides read it.

# SERVER (5090): free read-only HuggingFace token — avoids rate-limiting on the
# Qwen3-TTS model pull. Export it before launching tts_server.py (or source this).
HF_TOKEN=

# CLIENT (Mac, bot.py): OPTIONAL — use Claude as the negotiation brain instead of
# the local Ollama model on the GPU. Billed to your account (NOT reimbursed).
# Leave blank to use the free Ollama brain.
ANTHROPIC_API_KEY=
